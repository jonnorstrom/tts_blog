<h2>One specific post</h2>

<h3><%= @post.title %> by: <%= @post.user.username %></h3>
<p><%= @post.body %></p>
<p>Date Written: <%= @post.created_at %></p>

<%= link_to 'New Comment', new_comment_path(@post) %>

<p>Comments:</p>
<ul>
  <% @post.comments.each do |comment| %>
    <li><%= comment.user.username%> : <%= comment.content %></li>
  <% end %>
</ul>
